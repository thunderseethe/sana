# Calc example for Sana

This is a simple example of using Sana as a lexer and LALRPOP as parser.

## Usage

```
cargo run --package calc
```

It will read the input and interpret it as an arithmetic expression.

Example:

```bash
$ cargo run --package calc
>> 1+2
3
```
